NODE_ENV=production
BACKEND_URL=http://backend:8000
# RUNNER_TOKEN es un secreto operativo del worker.
# Exportarlo solo al arrancar el profile automation o cargarlo desde
# un archivo local no versionado con permisos restringidos.
WORKER_POLL_INTERVAL_MS=5000
