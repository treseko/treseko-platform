export type AcceptanceCriterion = { local_id: string; type: string; title: string; given: string; when: string; then: string[]; observable_result: string; mandatory: boolean; source_refs: string[]; assumption_ids: string[] };
export type StoryProposal = { local_id: string; story_type: string; title: string; actor: string; goal: string; benefit: string; description: string; source_refs: string[]; assumption_ids: string[]; open_questions: string[]; acceptance_criteria: AcceptanceCriterion[]; quality: { testability: "PASS" | "WARN" | "FAIL"; warnings: string[] }; similar_stories?: { id: string; codigo: string; titulo: string; kind: "EXACT" | "SIMILAR" | "AI_INTENT"; score?: number; confidence?: "HIGH" | "MEDIUM"; reason?: string }[]; similarity_check?: { mode: "AI_INTENT" | "TITLE_FALLBACK"; status: "COMPLETED" | "UNAVAILABLE"; message?: string }; selected?: boolean; prioridad?: string; quality_override_accepted?: boolean; quality_override_reason?: string };
export type StoryGeneration = { id: string; estado: string; estimacion: Record<string, any>; analysis: Record<string, any>; propuestas: StoryProposal[]; warnings: any[]; generation_progress?: Record<string, any>; preflight_duplicate_check?: { requested?: number; planned?: number; excluded_existing_intent?: { title: string; reason?: string; similar_stories: StoryProposal["similar_stories"] }[]; mode?: "AI_INTENT" | "TITLE_FALLBACK" } };
